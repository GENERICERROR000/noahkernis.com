import React from 'react';
import PropTypes from 'prop-types';
import { withStyles } from '@material-ui/core/styles';

const styles = theme => ({});

class SuperSecretPage extends React.Component {
  render() {

    return (
      <div>
        LINK TO YOUTUBE PAGE YA FOOLS
      </div>
    );
  }
}

// SuperSecretPage.propTypes = {
//   classes: PropTypes.object.isRequired,
// };

export default withStyles(styles)(SuperSecretPage)
